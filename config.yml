language: vi
pipeline: "supervised_embeddings"

policies:
  - name: KerasPolicy
    epochs: 250
    max_history: 3
  - name: MemoizationPolicy
    max_history: 3
  - name: FallbackPolicy
    nlu_threshold: 0.1
    core_threshold: 0.2
    fallback_action_name: 'utter_default'
  - name: FormPolicy